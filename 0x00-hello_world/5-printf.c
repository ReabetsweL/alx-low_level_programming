#include <stadio.h>
/**
 * main -entry of program
 *
 * Return: zero if no errors, non-zero if error
 */
int main(void)
{

	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
